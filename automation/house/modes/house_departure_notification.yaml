- id: house_departure_notification
  alias: "House - Departure Notification"
  description: "Both phones left home. Actionable notification to select mode."
  trigger:
    - platform: state
      entity_id: person.edward
      from: "home"
      to: "not_home"
    - platform: state
      entity_id: person.loretta
      from: "home"
      to: "not_home"
  condition:
    - condition: state
      entity_id: person.edward
      state: "not_home"
    - condition: state
      entity_id: person.loretta
      state: "not_home"
    - condition: not
      conditions:
        - condition: state
          entity_id: input_select.house
          state: "Away"
  action:
    - service: notify.mobile_app_ee_iphone_16_pro_max
      data:
        title: "🏠 Everyone's Out"
        message: "Both phones have left. Select a mode:"
        data:
          actions:
            - action: "SET_ALL_OUT"
              title: "All Out"
            - action: "SET_DOGS_ALONE"
              title: "Dogs Alone"
            - action: "SET_AWAY"
              title: "Away"
